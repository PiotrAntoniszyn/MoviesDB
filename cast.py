class Cast():
    """docstring for Person."""
    def __init__(self, cast_id, movie_id, person_id, if_actor):
        # super(Person, self).__init__()
        self.cast_id = cast_id
        self.movie_id = movie_id
        self.person_id = person_id
        self.if_actor = if_actor
