class Rating:
    """docstring for movie."""
    def __init__(self,rate_id, movie_id, rate,userName):
        # super(movie, self).__init__()
        self.rate_id = rate_id
        self.movie_id = movie_id
        self.rate = rate
        self.userName = userName
