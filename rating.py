class Rating:
    """docstring for movie."""
    def __init__(self,rate_id, movie_id, rate,user_name):
        # super(movie, self).__init__()
        self.rate_id = rate_id
        self.movie_id = movie_id
        self.rate = rate
        self.user_name = user_name
